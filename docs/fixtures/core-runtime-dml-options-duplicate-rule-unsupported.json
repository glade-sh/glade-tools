{
  "name": "core-runtime-dml-options-duplicate-rule-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:Database.DMLOptions.DuplicateRuleHeader",
      "symbol": "Database.DMLOptions.DuplicateRuleHeader",
      "kind": "unsupported",
      "notes": "Configured DuplicateRuleHeader returns the stable local UnsupportedFeature boundary before local DML."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Database.DMLOptions opts = new Database.DMLOptions();\nopts.DuplicateRuleHeader.AllowSave = true;\nDatabase.insert(new Account(Name = 'Rejected'), opts);\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Database.DMLOptions opts = new Database.DMLOptions();\nopts.DuplicateRuleHeader.AllowSave = true;\nDatabase.insert(new Account(Name = 'Rejected'), opts);"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Database.DMLOptions.DuplicateRuleHeader local DML option behavior\""
    }
  }
}
