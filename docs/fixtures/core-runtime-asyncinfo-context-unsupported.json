{
  "name": "core-runtime-asyncinfo-context-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:System.AsyncInfo.hasMaxStackDepth()",
      "symbol": "AsyncInfo.hasMaxStackDepth",
      "kind": "unsupported",
      "notes": "Local execution has no Queueable or Finalizer context for stack-depth metadata."
    },
    {
      "surfaceId": "apex:System.AsyncInfo.getCurrentQueueableStackDepth()",
      "symbol": "AsyncInfo.getCurrentQueueableStackDepth",
      "kind": "unsupported",
      "notes": "Local execution has no Queueable or Finalizer context for stack-depth metadata."
    },
    {
      "surfaceId": "apex:System.AsyncInfo.getMaximumQueueableStackDepth()",
      "symbol": "AsyncInfo.getMaximumQueueableStackDepth",
      "kind": "unsupported",
      "notes": "Local execution has no Queueable or Finalizer context for stack-depth metadata."
    },
    {
      "surfaceId": "apex:System.AsyncInfo.getMinimumQueueableDelayInMinutes()",
      "symbol": "AsyncInfo.getMinimumQueueableDelayInMinutes",
      "kind": "unsupported",
      "notes": "Local execution has no Queueable or Finalizer context for queueable delay metadata."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Boolean caughtHasMax = false;\ntry {\n  AsyncInfo.hasMaxStackDepth();\n} catch (AsyncException e) {\n  caughtHasMax = e.getMessage().contains('hasMaxStackDepth');\n}\nSystem.assert(caughtHasMax);\nBoolean caughtCurrent = false;\ntry {\n  AsyncInfo.getCurrentQueueableStackDepth();\n} catch (AsyncException e) {\n  caughtCurrent = e.getMessage().contains('getCurrentQueueableStackDepth');\n}\nSystem.assert(caughtCurrent);\nBoolean caughtMaximum = false;\ntry {\n  AsyncInfo.getMaximumQueueableStackDepth();\n} catch (AsyncException e) {\n  caughtMaximum = e.getMessage().contains('getMaximumQueueableStackDepth');\n}\nSystem.assert(caughtMaximum);\nBoolean caughtMinimum = false;\ntry {\n  AsyncInfo.getMinimumQueueableDelayInMinutes();\n} catch (AsyncException e) {\n  caughtMinimum = e.getMessage().contains('getMinimumQueueableDelayInMinutes');\n}\nSystem.assert(caughtMinimum);\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Boolean caughtHasMax = false;\ntry {\n  AsyncInfo.hasMaxStackDepth();\n} catch (AsyncException e) {\n  caughtHasMax = e.getMessage().contains('hasMaxStackDepth');\n}\nSystem.assert(caughtHasMax);\nBoolean caughtCurrent = false;\ntry {\n  AsyncInfo.getCurrentQueueableStackDepth();\n} catch (AsyncException e) {\n  caughtCurrent = e.getMessage().contains('getCurrentQueueableStackDepth');\n}\nSystem.assert(caughtCurrent);\nBoolean caughtMaximum = false;\ntry {\n  AsyncInfo.getMaximumQueueableStackDepth();\n} catch (AsyncException e) {\n  caughtMaximum = e.getMessage().contains('getMaximumQueueableStackDepth');\n}\nSystem.assert(caughtMaximum);\nBoolean caughtMinimum = false;\ntry {\n  AsyncInfo.getMinimumQueueableDelayInMinutes();\n} catch (AsyncException e) {\n  caughtMinimum = e.getMessage().contains('getMinimumQueueableDelayInMinutes');\n}\nSystem.assert(caughtMinimum);\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
