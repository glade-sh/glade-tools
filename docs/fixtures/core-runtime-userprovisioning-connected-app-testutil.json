{
  "name": "core-runtime-userprovisioning-connected-app-testutil",
  "evidence": [
    {
      "surfaceId": "apex:UserProvisioning.ConnectorTestUtil.createConnectedApp(String)",
      "symbol": "UserProvisioning.ConnectorTestUtil.createConnectedApp(String)",
      "kind": "unsupported",
      "notes": "ConnectorTestUtil.createConnectedApp is a Salesforce test-only utility and rejects anonymous execution with System.TypeException; preserve the rejection rather than adding local behavior."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "ConnectedApplication app = UserProvisioning.ConnectorTestUtil.createConnectedApp('Local App');\nSystem.assertEquals('Local App', app.Name);\nSystem.assertEquals('0SO000000000001', app.Id);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "ConnectedApplication app = UserProvisioning.ConnectorTestUtil.createConnectedApp('Local App');\nSystem.assertEquals('Local App', app.Name);\nSystem.assertEquals('0SO000000000001', app.Id);"
    ]
  },
  "expected": {
    "error": {
      "type": "System.TypeException",
      "message": "Cannot call test methods in non-test context"
    }
  }
}
