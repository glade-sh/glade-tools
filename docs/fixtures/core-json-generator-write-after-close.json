{
  "name": "core-json-generator-write-after-close",
  "evidence": [
    {
      "surfaceId": "apex:System.JSONGenerator.getAsString()",
      "symbol": "JSONGenerator.getAsString",
      "kind": "exec",
      "notes": "getAsString closes the generator; later writes raise a catchable JSONException and leave the generator closed."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartArray();\ngen.writeString('done');\ngen.writeEndArray();\nSystem.assertEquals('[\"done\"]', gen.getAsString());\nSystem.assert(gen.isClosed());\nBoolean caught = false;\ntry {\n    gen.writeNull();\n} catch (JSONException e) {\n    caught = true;\n    System.assertEquals('System.JSONException', e.getTypeName());\n    System.assert(e.getMessage().contains('JSONGenerator is closed'));\n}\nSystem.assert(caught);\nSystem.assert(gen.isClosed());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartArray();\ngen.writeString('done');\ngen.writeEndArray();\nSystem.assertEquals('[\"done\"]', gen.getAsString());\nSystem.assert(gen.isClosed());\nBoolean caught = false;\ntry {\n    gen.writeNull();\n} catch (JSONException e) {\n    caught = true;\n    System.assertEquals('System.JSONException', e.getTypeName());\n    System.assert(e.getMessage().contains('JSONGenerator is closed'));\n}\nSystem.assert(caught);\nSystem.assert(gen.isClosed());"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
