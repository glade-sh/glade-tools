{
  "salesforceEligible": false,
  "salesforceExclusionClass": "policy-local-only",
  "salesforceExclusionReason": "The fixture proves Glade's in-memory clone-source bookkeeping from a fabricated record ID, not a hosted Salesforce record-clone observation, and grants zero Salesforce parity credit.",
  "name": "core-runtime-sobject-clone-source",
  "evidence": [
    {
      "surfaceId": "apex:System.SObject.isClone()",
      "symbol": "SObject.isClone",
      "kind": "exec",
      "notes": "SObject.clone marks the returned local record as a clone while leaving the source record unmarked."
    },
    {
      "surfaceId": "apex:System.SObject.getCloneSourceId()",
      "symbol": "SObject.getCloneSourceId",
      "kind": "exec",
      "notes": "SObject.clone records the source Id for clone-source access."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "SObject source = new Account(Id = '001B000001DVM9tIAH', Name = 'Acme');\nSystem.assertEquals(false, source.isClone());\nSystem.assertEquals(null, source.getCloneSourceId());\nSObject cloneNoId = source.clone(false, true, false, false);\nSystem.assertEquals(true, cloneNoId.isClone());\nSystem.assertEquals(source.get('Id'), cloneNoId.getCloneSourceId());\nSystem.assertEquals(null, cloneNoId.get('Id'));\nSObject cloneWithId = source.clone(true, true, false, false);\nSystem.assertEquals(true, cloneWithId.isClone());\nSystem.assertEquals(source.get('Id'), cloneWithId.get('Id'));\nSystem.assertEquals(source.get('Id'), cloneWithId.getCloneSourceId());\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "SObject source = new Account(Id = '001B000001DVM9tIAH', Name = 'Acme');\nSystem.assertEquals(false, source.isClone());\nSystem.assertEquals(null, source.getCloneSourceId());\nSObject cloneNoId = source.clone(false, true, false, false);\nSystem.assertEquals(true, cloneNoId.isClone());\nSystem.assertEquals(source.get('Id'), cloneNoId.getCloneSourceId());\nSystem.assertEquals(null, cloneNoId.get('Id'));\nSObject cloneWithId = source.clone(true, true, false, false);\nSystem.assertEquals(true, cloneWithId.isClone());\nSystem.assertEquals(source.get('Id'), cloneWithId.get('Id'));\nSystem.assertEquals(source.get('Id'), cloneWithId.getCloneSourceId());\n"
    ]
  },
  "expected": {
    "stdout": ""
  }
}
