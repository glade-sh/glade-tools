{
  "name": "core-numeric-stdlib-unsupported-decimal-format",
  "evidence": [
    {
      "symbol": "Decimal/Double.format overloads",
      "kind": "exec",
      "notes": "Locale/pattern numeric format overloads are not locally modeled and return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Decimal d = 7.25;\nd.format('en_US');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Decimal d = 7.25;\nd.format('en_US');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Decimal/Double.format locale/pattern overloads\""
    }
  }
}