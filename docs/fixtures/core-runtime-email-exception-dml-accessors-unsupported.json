{
  "name": "core-runtime-email-exception-dml-accessors-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:System.EmailException.getDmlFieldNames(Integer)",
      "symbol": "EmailException.getDmlFieldNames(Integer)",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    },
    {
      "surfaceId": "apex:System.EmailException.getDmlFields(Integer)",
      "symbol": "EmailException.getDmlFields(Integer)",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    },
    {
      "surfaceId": "apex:System.EmailException.getDmlId(Integer)",
      "symbol": "EmailException.getDmlId(Integer)",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    },
    {
      "surfaceId": "apex:System.EmailException.getDmlIndex(Integer)",
      "symbol": "EmailException.getDmlIndex(Integer)",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    },
    {
      "surfaceId": "apex:System.EmailException.getDmlMessage(Integer)",
      "symbol": "EmailException.getDmlMessage(Integer)",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    },
    {
      "surfaceId": "apex:System.EmailException.getDmlStatusCode(Integer)",
      "symbol": "EmailException.getDmlStatusCode(Integer)",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    },
    {
      "surfaceId": "apex:System.EmailException.getDmlType(Integer)",
      "symbol": "EmailException.getDmlType(Integer)",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    },
    {
      "surfaceId": "apex:System.EmailException.getNumDml()",
      "symbol": "EmailException.getNumDml",
      "kind": "unsupported",
      "notes": "DML detail accessors are modeled only for DmlException; EmailException keeps the standard Exception accessors."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "EmailException e = new EmailException('email');\ne.getNumDml();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "EmailException e = new EmailException('email');\ne.getNumDml();"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"e.getNumDml\""
    }
  }
}
