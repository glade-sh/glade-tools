{
  "name": "core-numeric-stdlib-unsupported-long-format",
  "evidence": [
    {
      "symbol": "Integer/Long.format overloads",
      "kind": "exec",
      "notes": "Locale/pattern numeric format overloads are not locally modeled and return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Long l = 7;\nl.format('en_US');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Long l = 7;\nl.format('en_US');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Integer/Long.format locale/pattern overloads\""
    }
  }
}