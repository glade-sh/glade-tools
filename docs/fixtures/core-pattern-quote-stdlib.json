{
  "name": "core-pattern-quote-stdlib",
  "evidence": [
    {
      "surfaceId": "apex:System.Pattern.quote(String)",
      "symbol": "Pattern.quote",
      "kind": "exec",
      "notes": "Local Pattern.quote returns regex escaping so common metacharacters match literally with local Pattern/Matcher."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "String literal = 'a+b?(x)[1]';\nString quoted = Pattern.quote(literal);\nSystem.assert(Pattern.matches(quoted, literal));\nSystem.assert(!Pattern.matches(quoted, 'abx1'));\nMatcher literalMatcher = Pattern.compile(quoted).matcher('pre a+b?(x)[1] post');\nSystem.assert(literalMatcher.find());\nSystem.assertEquals(literal, literalMatcher.group());\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "String literal = 'a+b?(x)[1]';\nString quoted = Pattern.quote(literal);\nSystem.assert(Pattern.matches(quoted, literal));\nSystem.assert(!Pattern.matches(quoted, 'abx1'));\nMatcher literalMatcher = Pattern.compile(quoted).matcher('pre a+b?(x)[1] post');\nSystem.assert(literalMatcher.find());\nSystem.assertEquals(literal, literalMatcher.group());\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
