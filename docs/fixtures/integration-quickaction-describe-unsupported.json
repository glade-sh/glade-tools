{
  "name": "integration-quickaction-describe-unsupported",
  "evidence": [
    {
      "symbol": "QuickAction.describeAvailableActions",
      "kind": "exec",
      "notes": "Quick action discovery is a UI/platform metadata surface; the local VM returns UnsupportedFeature instead of synthetic actions."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "QuickAction.describeAvailableActions('Account');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "QuickAction.describeAvailableActions('Account');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"QuickAction.describeAvailableActions local quick action UI surface\""
    }
  }
}