{
  "name": "integration-quickaction-describe-local",
  "evidence": [
    {
      "symbol": "QuickAction.describeAvailableActions",
      "kind": "exec",
      "notes": "Quick action discovery returns deterministic local metadata DTOs without live UI action service execution."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "QuickAction.describeAvailableActions('Account');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "QuickAction.describeAvailableActions('Account');"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
