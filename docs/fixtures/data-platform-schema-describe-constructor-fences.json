{
  "name": "data-platform-schema-describe-constructor-fences",
  "evidence": [
    {
      "symbol": "Schema.ChildRelationship constructor",
      "surfaceId": "apex:Schema.ChildRelationship.ChildRelationship()",
      "kind": "exec",
      "notes": "Describe result values come from Schema describe calls; direct construction is not a supported Apex contract."
    },
    {
      "symbol": "Schema.DescribeFieldResult constructor",
      "surfaceId": "apex:Schema.DescribeFieldResult.DescribeFieldResult()",
      "kind": "exec",
      "notes": "Describe result values come from Schema describe calls; direct construction is not a supported Apex contract."
    },
    {
      "symbol": "Schema.SObjectType constructor",
      "surfaceId": "apex:Schema.SObjectType.SObjectType()",
      "kind": "exec",
      "notes": "SObject type tokens are platform-provided; direct construction is not a supported Apex contract."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "new Schema.DescribeFieldResult();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "new Schema.DescribeFieldResult();"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Schema.DescribeFieldResult constructor\""
    }
  }
}
