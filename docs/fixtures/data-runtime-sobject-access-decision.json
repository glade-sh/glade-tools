{
  "name": "data-runtime-sobject-access-decision",
  "evidence": [
    {
      "surfaceId": "apex:System.Security.stripInaccessible(AccessType,List<Object>)",
      "symbol": "Security.stripInaccessible",
      "kind": "test",
      "notes": "Security.stripInaccessible strips fields for local records using the current user's object and field permissions."
    }
  ],
  "source": [
    {
      "path": "force-app/main/classes/DataRuntimeSObjectAccessDecisionTest.cls",
      "content": "@isTest private class DataRuntimeSObjectAccessDecisionTest { @isTest static void stripInaccessibleReturnsDecisionDetails() { List<Account> rows = new List<Account>{new Account(Name = 'Probe')}; SObjectAccessDecision decision = Security.stripInaccessible(AccessType.READABLE, rows, false); System.assertEquals(1, decision.getRecords().size()); System.assertEquals(0, decision.getRemovedFields().size()); System.assertEquals(0, decision.getModifiedIndexes().size()); } }"
    }
  ],
  "command": {
    "kind": "test"
  },
  "expected": {
    "result": {
      "ok": true,
      "total": 1,
      "passed": 1,
      "failed": 0,
      "errors": 0
    }
  }
}
