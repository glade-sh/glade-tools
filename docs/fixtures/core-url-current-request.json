{
  "name": "core-url-current-request",
  "salesforceEligible": false,
  "salesforceExclusionClass": "policy-local-only",
  "salesforceExclusionReason": "The command asserts Glade's synthetic local request URL rather than a Salesforce request-context URL; this fixture is policy-local-only and grants zero Salesforce parity credit.",
  "evidence": [
    {
      "surfaceId": "apex:System.URL.getCurrentRequestUrl()",
      "symbol": "URL.getCurrentRequestUrl",
      "kind": "exec",
      "notes": "URL.getCurrentRequestUrl returns the local current request URL."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "URL current = URL.getCurrentRequestUrl();\nSystem.assertEquals('https://local.glade.example/apex/current', current.toExternalForm());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "URL current = URL.getCurrentRequestUrl();\nSystem.assertEquals('https://local.glade.example/apex/current', current.toExternalForm());"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
