{
  "name": "core-timezone-unsupported-named-zone",
  "evidence": [
    {
      "symbol": "TimeZone.getTimeZone",
      "kind": "exec",
      "notes": "Named timezone IDs outside the deterministic deterministic named-zone table return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "TimeZone.getTimeZone('America/Phoenix');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "TimeZone.getTimeZone('America/Phoenix');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"TimeZone.getTimeZone America/Phoenix\""
    }
  }
}