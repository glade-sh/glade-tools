{
  "name": "ui-pagereference-getcontentaspdf-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:System.PageReference.getContentAsPDF()",
      "symbol": "PageReference.getContentAsPDF",
      "kind": "unsupported",
      "notes": "Visualforce PDF generation is outside the no-render local Apex controller surface."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "PageReference page = new PageReference('/apex/Trail');\npage.getContentAsPDF();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "PageReference page = new PageReference('/apex/Trail');\npage.getContentAsPDF();"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"PageReference.getContentAsPDF local Visualforce page rendering surface\""
    }
  }
}
