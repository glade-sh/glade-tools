{
  "name": "system-assert-class-aliases",
  "evidence": [
    {
      "symbol": "Assert.areEqual",
      "kind": "exec",
      "notes": "System.Assert class aliases route through the existing System assertion behavior with case-insensitive static dispatch."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "System.Assert.areEqual(2, 1 + 1);\nSystem.Assert.areNotEqual(3, 1 + 1);\nSystem.Assert.isTrue(1 < 2);\nSystem.Assert.isFalse(2 < 1);\nSystem.Assert.isNull(null);\nSystem.Assert.isNotNull('value');\nSYSTEM.assert.AREEQUAL('trail', 'trail');\nAssert.areEqual('short', 'short');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "System.Assert.areEqual(2, 1 + 1);\nSystem.Assert.areNotEqual(3, 1 + 1);\nSystem.Assert.isTrue(1 < 2);\nSystem.Assert.isFalse(2 < 1);\nSystem.Assert.isNull(null);\nSystem.Assert.isNotNull('value');\nSYSTEM.assert.AREEQUAL('trail', 'trail');\nAssert.areEqual('short', 'short');"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
