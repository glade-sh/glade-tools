{
  "name": "async-finalizer-registration-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:System.System.attachFinalizer(Object)",
      "symbol": "System.attachFinalizer",
      "kind": "exec",
      "notes": "Queueable finalizer registration is fenced as UnsupportedFeature; no local finalizer lifecycle is faked."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "System.attachFinalizer(null);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "System.attachFinalizer(null);"
    ]
  },
  "expected": {
    "error": {
      "type": "Error",
      "message": "System.attachFinalizer expects Finalizer object"
    }
  }
}
