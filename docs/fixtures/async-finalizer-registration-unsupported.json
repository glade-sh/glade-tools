{
  "name": "async-finalizer-registration-outside-queueable",
  "evidence": [
    {
      "surfaceId": "apex:System.System.attachFinalizer(Object)",
      "symbol": "System.attachFinalizer",
      "kind": "exec",
      "notes": "Salesforce rejects finalizer registration outside Queueable execution with a handled context error."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "System.attachFinalizer(null);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "System.attachFinalizer(null);"
    ]
  },
  "expected": {
    "error": {
      "type": "System.HandledException",
      "message": "System.attachFinalizer(Finalizer) is not allowed in this context"
    }
  }
}
