{
  "name": "core-encoding-url-ascii-invalid",
  "evidence": [
    {
      "symbol": "EncodingUtil.urlEncode UTF-8/ASCII/Latin-1",
      "kind": "exec",
      "notes": "US-ASCII URL encoding rejects non-ASCII code points instead of silently replacing them."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "EncodingUtil.urlEncode('é', 'US-ASCII');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "EncodingUtil.urlEncode('é', 'US-ASCII');"
    ]
  },
  "expected": {
    "error": {
      "type": "System.StringException",
      "message": "EncodingUtil.urlEncode charset \"US-ASCII\" cannot encode U+00E9"
    }
  }
}
