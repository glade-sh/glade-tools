{
  "name": "http-real-transport-unsupported",
  "evidence": [
    {
      "symbol": "Http.send real network transport",
      "kind": "exec",
      "notes": "Real network transport is not locally modeled."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "HttpRequest req = new HttpRequest(); req.setEndpoint('https://example.test'); req.setMethod('GET'); Http h = new Http(); h.send(req);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "HttpRequest req = new HttpRequest(); req.setEndpoint('https://example.test'); req.setMethod('GET'); Http h = new Http(); h.send(req);"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Http.send real network transport\""
    }
  }
}