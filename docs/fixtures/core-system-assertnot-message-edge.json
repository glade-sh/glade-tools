{
  "name": "core-system-assertnot-message-edge",
  "evidence": [
    {
      "surfaceId": "apex:System.System.assertNotEquals(Object,Object)",
      "symbol": "System.assertNotEquals",
      "kind": "exec",
      "notes": "Assertion failure uses deterministic local value text and exception object message conversion."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "System.assertNotEquals('same', 'same', new DmlException('duplicate'));"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "System.assertNotEquals('same', 'same', new DmlException('duplicate'));"
    ]
  },
  "expected": {
    "error": {
      "type": "System.AssertException",
      "message": "values should not be equal: <same>: System.DmlException: duplicate"
    }
  }
}
