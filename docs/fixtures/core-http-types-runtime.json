{
  "name": "core-http-types-runtime",
  "evidence": [
    {
      "surfaceId": "apex:System.HttpRequest",
      "symbol": "HttpRequest",
      "kind": "exec",
      "notes": "An HTTP request can be constructed for local execution."
    },
    {
      "surfaceId": "apex:System.HttpResponse",
      "symbol": "HttpResponse",
      "kind": "exec",
      "notes": "An HTTP response can be constructed for local execution."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "HttpRequest requestValue = new HttpRequest(); HttpResponse responseValue = new HttpResponse(); System.assertNotEquals(null, requestValue); System.assertNotEquals(null, responseValue);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "HttpRequest requestValue = new HttpRequest(); HttpResponse responseValue = new HttpResponse(); System.assertNotEquals(null, requestValue); System.assertNotEquals(null, responseValue);"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
