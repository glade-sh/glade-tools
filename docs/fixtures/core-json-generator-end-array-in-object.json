{
  "name": "core-json-generator-end-array-in-object",
  "evidence": [
    {
      "surfaceId": "apex:System.JSONGenerator.writeEndArray()",
      "symbol": "JSONGenerator.writeEndArray",
      "kind": "exec",
      "notes": "Ending an array while an object is open raises a catchable JSONException and leaves the generator usable for object fields."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartObject();\nBoolean caught = false;\ntry {\n    gen.writeEndArray();\n} catch (JSONException e) {\n    caught = true;\n    System.assertEquals('System.JSONException', e.getTypeName());\n    System.assert(e.getMessage().contains('writeEndArray cannot be called inside an object'));\n}\nSystem.assert(caught);\ngen.writeStringField('ok', 'yes');\ngen.writeEndObject();\nSystem.assertEquals('{\"ok\":\"yes\"}', gen.getAsString());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartObject();\nBoolean caught = false;\ntry {\n    gen.writeEndArray();\n} catch (JSONException e) {\n    caught = true;\n    System.assertEquals('System.JSONException', e.getTypeName());\n    System.assert(e.getMessage().contains('writeEndArray cannot be called inside an object'));\n}\nSystem.assert(caught);\ngen.writeStringField('ok', 'yes');\ngen.writeEndObject();\nSystem.assertEquals('{\"ok\":\"yes\"}', gen.getAsString());"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
