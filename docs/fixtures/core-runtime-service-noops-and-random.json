{
  "name": "core-runtime-service-noops-and-random",
  "salesforceEligible": false,
  "salesforceExclusionClass": "policy-local-only",
  "salesforceExclusionReason": "This fixture proves deterministic local no-op services and pseudo-random values, not hosted side effects or entropy; it grants zero Salesforce parity credit.",
  "evidence": [
    {
      "surfaceId": "apex:System.Aura.redirect(Object)",
      "symbol": "Aura.redirect(Object)",
      "kind": "exec",
      "notes": "Local Aura.redirect accepts a PageReference target and performs no navigation side effect."
    },
    {
      "surfaceId": "apex:System.BcpProvisionService.enableC2C()",
      "symbol": "BcpProvisionService.enableC2C",
      "kind": "exec",
      "notes": "Local provisioning hook is a no-op."
    },
    {
      "surfaceId": "apex:System.Crypto.getRandomInteger()",
      "symbol": "Crypto.getRandomInteger",
      "kind": "exec",
      "notes": "Local runtime uses a deterministic pseudo-random sequence."
    },
    {
      "surfaceId": "apex:System.Crypto.getRandomLong()",
      "symbol": "Crypto.getRandomLong",
      "kind": "exec",
      "notes": "Local runtime uses a deterministic pseudo-random sequence."
    },
    {
      "surfaceId": "apex:System.DistributedLedgerService.enableC2C()",
      "symbol": "DistributedLedgerService.enableC2C",
      "kind": "exec",
      "notes": "Local provisioning hook is a no-op."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Aura.redirect(new PageReference('/redirect'));\nBcpProvisionService.enableC2C();\nDistributedLedgerService.enableC2C();\nLong first = Crypto.getRandomLong();\nLong second = Crypto.getRandomLong();\nSystem.assertEquals(-2152535657050944081, first);\nSystem.assertEquals(7960286522194355700, second);\nSystem.assertNotEquals(first, second);\nInteger firstInteger = Crypto.getRandomInteger();\nInteger secondInteger = Crypto.getRandomInteger();\nSystem.assertEquals(-2146876081, firstInteger);\nSystem.assertEquals(1917616620, secondInteger);\nSystem.assertNotEquals(firstInteger, secondInteger);\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Aura.redirect(new PageReference('/redirect'));\nBcpProvisionService.enableC2C();\nDistributedLedgerService.enableC2C();\nLong first = Crypto.getRandomLong();\nLong second = Crypto.getRandomLong();\nSystem.assertEquals(-2152535657050944081, first);\nSystem.assertEquals(7960286522194355700, second);\nSystem.assertNotEquals(first, second);\nInteger firstInteger = Crypto.getRandomInteger();\nInteger secondInteger = Crypto.getRandomInteger();\nSystem.assertEquals(-2146876081, firstInteger);\nSystem.assertEquals(1917616620, secondInteger);\nSystem.assertNotEquals(firstInteger, secondInteger);\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
