{
  "name": "examples-http-mock",
  "evidence": [
    {
      "surfaceId": "apex:System.Test.setMock(Type,Object)",
      "symbol": "Test.setMock",
      "kind": "check",
      "notes": "Reduced HTTP mock with HttpCalloutMock implementation."
    }
  ],
  "source": [
    {
      "path": "classes/MyHttpMock.cls",
      "content": "public class MyHttpMock implements HttpCalloutMock {\n  public HTTPResponse respond(HTTPRequest request) {\n    HTTPResponse response = new HTTPResponse();\n    response.setStatusCode(200);\n    response.setBody('{}');\n    return response;\n  }\n}"
    },
    {
      "path": "classes/HttpClient.cls",
      "content": "public class HttpClient {\n  public String fetch(String endpoint) {\n    HttpRequest request = new HttpRequest();\n    request.setEndpoint(endpoint);\n    request.setMethod('GET');\n    Http http = new Http();\n    HttpResponse response = http.send(request);\n    return response.getBody();\n  }\n}"
    },
    {
      "path": "classes/HttpClientTest.cls",
      "content": "@isTest private class HttpClientTest {\n  @isTest static void fetchesSuccessfully() {\n    Test.setMock(HttpCalloutMock.class, new MyHttpMock());\n    HttpClient client = new HttpClient();\n    String body = client.fetch('http://example.com/api');\n    System.assertEquals('{}', body);\n  }\n}"
    }
  ],
  "command": {
    "kind": "check"
  },
  "expected": {
    "result": {
      "diagnostics": 0,
      "files": 3,
      "ok": true,
      "types": 3
    }
  }
}
