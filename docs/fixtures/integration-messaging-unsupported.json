{
  "name": "integration-messaging-unsupported",
  "evidence": [
    {
      "symbol": "Messaging.sendPushNotification",
      "kind": "exec",
      "notes": "Messaging transport APIs outside the local captured sendEmail, sendEmailMessage, custom notification, and push notification instance harnesses return UnsupportedFeature diagnostics."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Messaging.sendPushNotification(new List<String>{'005000000000001'}, 'payload');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Messaging.sendPushNotification(new List<String>{'005000000000001'}, 'payload');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Messaging.sendPushNotification local messaging transport/template surface\""
    }
  }
}
