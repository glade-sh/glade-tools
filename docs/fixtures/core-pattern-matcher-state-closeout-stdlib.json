{
  "name": "core-pattern-matcher-state-closeout-stdlib",
  "evidence": [
    {
      "surfaceId": "apex:System.Matcher.group(Integer)",
      "symbol": "Matcher.group",
      "kind": "exec",
      "notes": "Optional unmatched groups return null while start/end return -1 after a successful local match."
    },
    {
      "symbol": "Matcher.matches",
      "kind": "exec",
      "notes": "Failed full-region matches clear stale group state before later successful matches."
    },
    {
      "surfaceId": "apex:System.Matcher.lookingAt()",
      "symbol": "Matcher.lookingAt",
      "kind": "exec",
      "notes": "Failed leading matches clear stale group state and later find() repopulates it."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern optionalPattern = Pattern.compile('([A-Z]+)([0-9]+)?');\nMatcher optional = optionalPattern.matcher('ABC');\nSystem.assert(optional.matches());\nSystem.assertEquals(null, optional.group(2));\nSystem.assertEquals(-1, optional.start(2));\nSystem.assertEquals(-1, optional.end(2));\nPattern word = Pattern.compile('[A-Z]+');\nMatcher full = word.matcher('ABC def');\nSystem.assert(full.find());\nSystem.assertEquals('ABC', full.group());\nSystem.assert(!full.matches());\nfull.reset();\nSystem.assert(full.find());\nSystem.assertEquals('ABC', full.group());\nMatcher head = word.matcher('ABC def');\nSystem.assert(head.find());\nhead.reset('abc DEF');\nSystem.assert(!head.lookingAt());\nSystem.assert(head.find());\nSystem.assertEquals('DEF', head.group());\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern optionalPattern = Pattern.compile('([A-Z]+)([0-9]+)?');\nMatcher optional = optionalPattern.matcher('ABC');\nSystem.assert(optional.matches());\nSystem.assertEquals(null, optional.group(2));\nSystem.assertEquals(-1, optional.start(2));\nSystem.assertEquals(-1, optional.end(2));\nPattern word = Pattern.compile('[A-Z]+');\nMatcher full = word.matcher('ABC def');\nSystem.assert(full.find());\nSystem.assertEquals('ABC', full.group());\nSystem.assert(!full.matches());\nfull.reset();\nSystem.assert(full.find());\nSystem.assertEquals('ABC', full.group());\nMatcher head = word.matcher('ABC def');\nSystem.assert(head.find());\nhead.reset('abc DEF');\nSystem.assert(!head.lookingAt());\nSystem.assert(head.find());\nSystem.assertEquals('DEF', head.group());\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
