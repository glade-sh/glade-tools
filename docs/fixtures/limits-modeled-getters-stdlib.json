{
  "name": "limits-modeled-getters-stdlib",
  "evidence": [
    {
      "symbol": "Limits modeled getters",
      "kind": "exec",
      "notes": "Covers a representative deterministic local slice of modeled Limits getters and max getters."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "System.assertEquals(0, Limits.getQueries());\nSystem.assert(Limits.getLimitQueries() > 0);\nSystem.assertEquals(0, Limits.getDmlStatements());\nSystem.assert(Limits.getLimitDmlStatements() > 0);\nSystem.assert(Limits.getHeapSize() >= 0);\nSystem.assert(Limits.getLimitHeapSize() > 0);\nSystem.assertEquals(0, Limits.getCallouts());\nSystem.assert(Limits.getLimitCallouts() > 0);\nSystem.assertEquals(0, Limits.getFutureCalls());\nSystem.assert(Limits.getLimitFutureCalls() > 0);\nSystem.assertEquals(0, Limits.getQueueableJobs());\nSystem.assert(Limits.getLimitQueueableJobs() > 0);\nSystem.assertEquals(0, Limits.getEmailInvocations());\nSystem.assert(Limits.getLimitEmailInvocations() > 0);\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "System.assertEquals(0, Limits.getQueries());\nSystem.assert(Limits.getLimitQueries() > 0);\nSystem.assertEquals(0, Limits.getDmlStatements());\nSystem.assert(Limits.getLimitDmlStatements() > 0);\nSystem.assert(Limits.getHeapSize() >= 0);\nSystem.assert(Limits.getLimitHeapSize() > 0);\nSystem.assertEquals(0, Limits.getCallouts());\nSystem.assert(Limits.getLimitCallouts() > 0);\nSystem.assertEquals(0, Limits.getFutureCalls());\nSystem.assert(Limits.getLimitFutureCalls() > 0);\nSystem.assertEquals(0, Limits.getQueueableJobs());\nSystem.assert(Limits.getLimitQueueableJobs() > 0);\nSystem.assertEquals(0, Limits.getEmailInvocations());\nSystem.assert(Limits.getLimitEmailInvocations() > 0);\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}