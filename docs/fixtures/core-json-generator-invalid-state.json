{
  "name": "core-json-generator-invalid-state",
  "evidence": [
    {
      "surfaceId": "apex:System.JSONGenerator.getAsString()",
      "symbol": "JSONGenerator.getAsString",
      "kind": "exec",
      "notes": "Invalid generator finish with an open container returns a stable local diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartObject();\ngen.getAsString();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartObject();\ngen.getAsString();"
    ]
  },
  "expected": {
    "error": {
      "message": "JSONGenerator cannot close with open JSON containers"
    }
  }
}
