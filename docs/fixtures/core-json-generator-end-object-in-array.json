{
  "name": "core-json-generator-end-object-in-array",
  "evidence": [
    {
      "surfaceId": "apex:System.JSONGenerator.writeEndObject()",
      "symbol": "JSONGenerator.writeEndObject",
      "kind": "exec",
      "notes": "Ending an object while an array is open raises a catchable JSONException and leaves the generator usable for array values."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartArray();\nBoolean caught = false;\ntry {\n    gen.writeEndObject();\n} catch (JSONException e) {\n    caught = true;\n    System.assertEquals('System.JSONException', e.getTypeName());\n    System.assert(e.getMessage().contains('writeEndObject cannot be called inside an array'));\n}\nSystem.assert(caught);\ngen.writeString('ok');\ngen.writeEndArray();\nSystem.assertEquals('[\"ok\"]', gen.getAsString());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "JSONGenerator gen = JSON.createGenerator(false);\ngen.writeStartArray();\nBoolean caught = false;\ntry {\n    gen.writeEndObject();\n} catch (JSONException e) {\n    caught = true;\n    System.assertEquals('System.JSONException', e.getTypeName());\n    System.assert(e.getMessage().contains('writeEndObject cannot be called inside an array'));\n}\nSystem.assert(caught);\ngen.writeString('ok');\ngen.writeEndArray();\nSystem.assertEquals('[\"ok\"]', gen.getAsString());"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
