{
  "name": "async-database-allowcallouts-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:System.Database.insertAsync(Object,Database.AllowCallouts,AccessLevel)",
      "symbol": "Database.insertAsync(Object,Database.AllowCallouts,AccessLevel)",
      "kind": "unsupported",
      "notes": "AllowCallouts async DML overloads require service-backed async callout execution that the local DML alias does not model."
    },
    {
      "surfaceId": "apex:System.Database.insertAsync(List<Object>,Database.AllowCallouts,AccessLevel)",
      "symbol": "Database.insertAsync(List<Object>,Database.AllowCallouts,AccessLevel)",
      "kind": "unsupported",
      "notes": "AllowCallouts async DML overloads require service-backed async callout execution that the local DML alias does not model."
    },
    {
      "surfaceId": "apex:System.Database.updateAsync(Object,Database.AllowCallouts,AccessLevel)",
      "symbol": "Database.updateAsync(Object,Database.AllowCallouts,AccessLevel)",
      "kind": "unsupported",
      "notes": "AllowCallouts async DML overloads require service-backed async callout execution that the local DML alias does not model."
    },
    {
      "surfaceId": "apex:System.Database.updateAsync(List<Object>,Database.AllowCallouts,AccessLevel)",
      "symbol": "Database.updateAsync(List<Object>,Database.AllowCallouts,AccessLevel)",
      "kind": "unsupported",
      "notes": "AllowCallouts async DML overloads require service-backed async callout execution that the local DML alias does not model."
    },
    {
      "surfaceId": "apex:System.Database.deleteAsync(Object,Database.AllowCallouts,AccessLevel)",
      "symbol": "Database.deleteAsync(Object,Database.AllowCallouts,AccessLevel)",
      "kind": "unsupported",
      "notes": "AllowCallouts async DML overloads require service-backed async callout execution that the local DML alias does not model."
    },
    {
      "surfaceId": "apex:System.Database.deleteAsync(List<Object>,Database.AllowCallouts,AccessLevel)",
      "symbol": "Database.deleteAsync(List<Object>,Database.AllowCallouts,AccessLevel)",
      "kind": "unsupported",
      "notes": "AllowCallouts async DML overloads require service-backed async callout execution that the local DML alias does not model."
    }
  ],
  "source": [
    {
      "path": "force-app/main/default/objects/Account/Account.object-meta.xml",
      "content": "<CustomObject><label>Account</label><pluralLabel>Accounts</pluralLabel><sharingModel>ReadWrite</sharingModel></CustomObject>"
    },
    {
      "path": "force-app/main/default/objects/Account/fields/Name.field-meta.xml",
      "content": "<CustomField><fullName>Name</fullName><label>Name</label><type>Text</type><required>true</required></CustomField>"
    },
    {
      "path": "anonymous.apex",
      "content": "Database.insertAsync(new Account(Name = 'callout'), Database.AllowCallouts.ALLOW, AccessLevel.SYSTEM_MODE);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Database.insertAsync(new Account(Name = 'callout'), Database.AllowCallouts.ALLOW, AccessLevel.SYSTEM_MODE);"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Database.insertAsync AllowCallouts overload local async callout surface\""
    }
  }
}
