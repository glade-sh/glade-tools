{
  "name": "core-runtime-web-store-context-unsupported",
  "evidence": [
    {"surfaceId": "apex:System.WebStoreContext.getCommerceContext()", "symbol": "WebStoreContext.getCommerceContext", "kind": "unsupported", "notes": "Commerce context depends on live Web Store request state that the local runtime does not model."}
  ],
  "source": [
    {"path": "anonymous.apex", "content": "WebStoreContext.getCommerceContext();"}
  ],
  "command": {
    "kind": "exec",
    "args": ["WebStoreContext.getCommerceContext();"]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"WebStoreContext.getCommerceContext local commerce context service\""
    }
  }
}
