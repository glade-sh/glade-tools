{
  "name": "messaging-send-email-closeout",
  "evidence": [
    {
      "symbol": "Messaging.sendEmail local result model",
      "kind": "test",
      "notes": "Local sendEmail supports empty, single, mass, mixed, and Boolean allOrNothing list overloads; it returns ordered success results and increments email limits once per valid call."
    },
    {
      "symbol": "Messaging.sendEmail transport, templates, and SendEmailOptions",
      "kind": "test",
      "notes": "The supported row is intentionally limited to the local result model; transport/template/options surfaces remain explicit unsupported rows."
    }
  ],
  "source": [
    {
      "path": "force-app/main/classes/MessagingSendEmailCloseoutFixtureTest.cls",
      "content": "@isTest private class MessagingSendEmailCloseoutFixtureTest { @isTest static void localResultModel() { System.assertEquals(0, Limits.getEmailInvocations()); List<Messaging.SendEmailResult> emptyResults = Messaging.sendEmail(new List<Messaging.SingleEmailMessage>()); System.assertEquals(0, emptyResults.size()); System.assertEquals(1, Limits.getEmailInvocations()); Messaging.SingleEmailMessage single = new Messaging.SingleEmailMessage(); single.setToAddresses(new List<String>{'single@example.test'}); single.setPlainTextBody('hello'); Messaging.MassEmailMessage mass = new Messaging.MassEmailMessage(); mass.setTargetObjectIds(new List<String>{'003000000000001'}); mass.setTemplateId('00X000000000001'); List<Messaging.SendEmailResult> mixedResults = Messaging.sendEmail(new List<Object>{single, mass}, false); System.assertEquals(2, mixedResults.size()); System.assert(mixedResults.get(0).isSuccess()); System.assert(mixedResults.get(1).isSuccess()); System.assertEquals(0, mixedResults.get(0).getErrors().size()); System.assertEquals(0, mixedResults.get(1).getErrors().size()); System.assertEquals(2, Limits.getEmailInvocations()); List<Messaging.SendEmailResult> singleResults = Messaging.sendEmail(new List<Messaging.SingleEmailMessage>{single}, true); System.assertEquals(1, singleResults.size()); System.assert(singleResults.get(0).isSuccess()); System.assertEquals(3, Limits.getEmailInvocations()); } }"
    }
  ],
  "command": {
    "kind": "test"
  },
  "expected": {
    "result": {
      "ok": true,
      "total": 1,
      "passed": 1,
      "failed": 0,
      "errors": 0
    }
  }
}
