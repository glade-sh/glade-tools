{
  "name": "core-pattern-invalid-flag-combination-unsupported",
  "evidence": [
    {
      "symbol": "Pattern.compile",
      "kind": "exec",
      "notes": "Java-only regex flag combinations are fenced with a typed UnsupportedFeature diagnostic listing each unsupported flag."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern.compile('a', Pattern.UNIX_LINES + Pattern.COMMENTS + Pattern.CANON_EQ);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern.compile('a', Pattern.UNIX_LINES + Pattern.COMMENTS + Pattern.CANON_EQ);"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Pattern.compile unsupported regex flags UNIX_LINES,COMMENTS,CANON_EQ\""
    }
  }
}