{
  "name": "core-json-strict-duplicate-fields",
  "evidence": [
    {
      "symbol": "JSON.deserializeStrict",
      "kind": "exec",
      "notes": "Strict typed deserialization rejects duplicate object fields before local SObject mapping silently keeps the last value."
    }
  ],
  "schema": [
    {
      "path": "force-app/main/default/objects/Account/Account.object-meta.xml",
      "content": "<CustomObject><label>Account</label><pluralLabel>Accounts</pluralLabel><sharingModel>ReadWrite</sharingModel></CustomObject>"
    },
    {
      "path": "force-app/main/default/objects/Account/fields/Name.field-meta.xml",
      "content": "<CustomField><fullName>Name</fullName><label>Name</label><type>Text</type></CustomField>"
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Account decoded = JSON.deserializeStrict('{\"Name\":\"First\",\"Name\":\"Second\"}', Account.class);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Account decoded = JSON.deserializeStrict('{\"Name\":\"First\",\"Name\":\"Second\"}', Account.class);"
    ]
  },
  "expected": {
    "error": {
      "message": "JSON.deserializeStrict found duplicate field \"Name\""
    }
  }
}