{
  "name": "core-string-split-empty-regex",
  "evidence": [
    {
      "symbol": "String.split",
      "kind": "exec",
      "notes": "String.split supports the empty regex with Apex trailing-empty semantics."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "String source = 'abc';\nList<String> parts = source.split('', -1);\nSystem.assertEquals(4, parts.size());\nSystem.assertEquals('a', parts.get(0));\nSystem.assertEquals('b', parts.get(1));\nSystem.assertEquals('c', parts.get(2));\nSystem.assertEquals('', parts.get(3));\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "String source = 'abc';\nList<String> parts = source.split('', -1);\nSystem.assertEquals(4, parts.size());\nSystem.assertEquals('a', parts.get(0));\nSystem.assertEquals('b', parts.get(1));\nSystem.assertEquals('c', parts.get(2));\nSystem.assertEquals('', parts.get(3));\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
