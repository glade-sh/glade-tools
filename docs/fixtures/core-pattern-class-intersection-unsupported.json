{
  "name": "core-pattern-class-intersection-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:System.Pattern.compile(String)",
      "symbol": "Pattern.compile",
      "kind": "unsupported",
      "notes": "Java character-class intersections/subtractions are not claimed on the Go-regexp backend and return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern.compile('[a-z&&[^aeiou]]+');\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern.compile('[a-z&&[^aeiou]]+');\n"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Pattern.compile Java regex character-class intersections\""
    }
  }
}
