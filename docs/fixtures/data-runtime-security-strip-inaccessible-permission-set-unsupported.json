{
  "name": "data-runtime-security-strip-inaccessible-permission-set-unsupported",
  "evidence": [
    {"surfaceId": "apex:System.Security.stripInaccessible(AccessType,List<Object>,Boolean,Id)", "symbol": "Security.stripInaccessible", "kind": "unsupported", "notes": "Permission-set-scoped field security checks are not modeled by the local runtime."}
  ],
  "source": [
    {"path": "anonymous.apex", "content": "Id permissionSetId = '0PS000000000001';\nSecurity.stripInaccessible(AccessType.READABLE, new List<Account>{ new Account(Name = 'Acme') }, true, permissionSetId);"}
  ],
  "command": {
    "kind": "exec",
    "args": ["Id permissionSetId = '0PS000000000001';\nSecurity.stripInaccessible(AccessType.READABLE, new List<Account>{ new Account(Name = 'Acme') }, true, permissionSetId);"]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Security.stripInaccessible permission-set-scoped access checks\""
    }
  }
}
