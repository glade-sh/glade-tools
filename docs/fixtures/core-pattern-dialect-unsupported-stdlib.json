{
  "name": "core-pattern-dialect-unsupported-stdlib",
  "evidence": [
    {
      "symbol": "Pattern.compile",
      "kind": "exec",
      "notes": "Pattern.COMMENTS flag constants remain outside the supported local flag subset and return a typed unsupported diagnostic; inline (?x) is supported separately."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern.compile('a b', Pattern.COMMENTS);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern.compile('a b', Pattern.COMMENTS);"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Pattern.compile unsupported regex flags COMMENTS\""
    }
  }
}
