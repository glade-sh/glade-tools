{
  "name": "async-execute-batch-scope-validation",
  "salesforceEligible": false,
  "salesforceExclusionClass": "policy-local-only",
  "salesforceExclusionReason": "The anonymous command references an unmaterialized BatchWorker and asserts Glade's local scope-validation error; this fixture is policy-local-only and grants zero Salesforce parity credit.",
  "evidence": [
    {
      "symbol": "Database.executeBatch",
      "surfaceId": "apex:System.Database.executeBatch(Object,Integer)",
      "kind": "exec",
      "notes": "Scope size above the public 2000 maximum is rejected before enqueue."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Database.executeBatch(new BatchWorker(), 2001);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Database.executeBatch(new BatchWorker(), 2001);"
    ]
  },
  "expected": {
    "error": {
      "type": "Error",
      "message": "Database.executeBatch scope size must be at most 2000"
    }
  }
}
