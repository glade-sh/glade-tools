{
  "name": "core-runtime-page-reference-redirect-code-evidence",
  "salesforceEligible": true,
  "evidence": [
    {
      "surfaceId": "apex:System.PageReference.getRedirectCode()",
      "symbol": "PageReference.getRedirectCode",
      "kind": "exec",
      "notes": "getRedirectCode returns the PageReference redirect code and defaults to 0."
    },
    {
      "surfaceId": "apex:System.PageReference.setRedirectCode(Integer)",
      "symbol": "PageReference.setRedirectCode",
      "kind": "exec",
      "notes": "setRedirectCode stores the redirect code and returns the same PageReference."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "PageReference page = new PageReference('/apex/Local');\nSystem.assertEquals(0, page.getRedirectCode());\nPageReference returned = page.setRedirectCode(301);\nSystem.assertEquals(page, returned);\nSystem.assertEquals(301, page.getRedirectCode());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "PageReference page = new PageReference('/apex/Local');\nSystem.assertEquals(0, page.getRedirectCode());\nPageReference returned = page.setRedirectCode(301);\nSystem.assertEquals(page, returned);\nSystem.assertEquals(301, page.getRedirectCode());"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
