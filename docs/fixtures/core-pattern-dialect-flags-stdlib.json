{
  "name": "core-pattern-dialect-flags-stdlib",
  "salesforceEligible": true,
  "evidence": [
    {
      "surfaceId": "apex:System.Pattern.pattern()",
      "symbol": "Pattern.pattern",
      "kind": "exec",
      "notes": "Pattern.pattern returns the original current regex source."
    },
    {
      "surfaceId": "apex:System.Matcher.find()",
      "symbol": "Matcher.find",
      "kind": "exec",
      "notes": "Find locates the next match from a current one-argument Pattern."
    },
    {
      "surfaceId": "apex:System.Matcher.matches()",
      "symbol": "Matcher.matches",
      "kind": "exec",
      "notes": "Matches honors a current inline case-insensitive regex."
    },
    {
      "surfaceId": "apex:System.Matcher.group()",
      "symbol": "Matcher.group",
      "kind": "exec",
      "notes": "Group returns the current match text."
    },
    {
      "surfaceId": "apex:System.Matcher.usePattern(Pattern)",
      "symbol": "Matcher.usePattern",
      "kind": "exec",
      "notes": "usePattern swaps a Matcher to another current compiled Pattern."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern inline = Pattern.compile('(?i)abc');\nSystem.assert(inline.matcher('ABC').matches());\nPattern word = Pattern.compile('[A-Z]+');\nMatcher wordMatcher = word.matcher('abc XYZ 9');\nSystem.assert(wordMatcher.find());\nSystem.assertEquals('XYZ', wordMatcher.group());\nPattern literal = Pattern.compile('a[.]b');\nSystem.assertEquals('a[.]b', literal.pattern());\nMatcher literalMatcher = literal.matcher('xx a.b axb 9');\nSystem.assert(literalMatcher.find());\nSystem.assertEquals('a.b', literalMatcher.group());\nliteralMatcher.usePattern(Pattern.compile('[0-9]+'));\nSystem.assert(literalMatcher.find());\nSystem.assertEquals('9', literalMatcher.group());\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern inline = Pattern.compile('(?i)abc');\nSystem.assert(inline.matcher('ABC').matches());\nPattern word = Pattern.compile('[A-Z]+');\nMatcher wordMatcher = word.matcher('abc XYZ 9');\nSystem.assert(wordMatcher.find());\nSystem.assertEquals('XYZ', wordMatcher.group());\nPattern literal = Pattern.compile('a[.]b');\nSystem.assertEquals('a[.]b', literal.pattern());\nMatcher literalMatcher = literal.matcher('xx a.b axb 9');\nSystem.assert(literalMatcher.find());\nSystem.assertEquals('a.b', literalMatcher.group());\nliteralMatcher.usePattern(Pattern.compile('[0-9]+'));\nSystem.assert(literalMatcher.find());\nSystem.assertEquals('9', literalMatcher.group());\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
