{
  "name": "core-pattern-split-nullable-unsupported",
  "evidence": [
    {
      "surfaceId": "apex:System.Pattern.split(String,Integer)",
      "symbol": "Pattern.split",
      "kind": "unsupported",
      "notes": "Nullable delimiter regexes are fenced because Java split preserves zero-width trailing pieces that Go regexp.Split cannot model safely."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern p = Pattern.compile('a*');\np.split('ab cd', -1);\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern p = Pattern.compile('a*');\np.split('ab cd', -1);\n"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Pattern.split regexes that can match empty strings\""
    }
  }
}
