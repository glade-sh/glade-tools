{
  "name": "core-pattern-dialect-unsupported-unicode-class",
  "evidence": [
    {
      "symbol": "Pattern.compile",
      "kind": "exec",
      "notes": "Java-specific Unicode character-class names are outside the local Go-regexp subset and return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern.compile('\\p{javaLowerCase}+');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern.compile('\\p{javaLowerCase}+');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Pattern.compile Java regex Unicode character classes\""
    }
  }
}