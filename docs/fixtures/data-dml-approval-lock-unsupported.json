{
  "name": "data-dml-approval-lock-unsupported",
  "evidence": [
    {
      "symbol": "Approval.lock missing org guard",
      "kind": "exec",
      "notes": "Approval.lock requires local org state; supported lock behavior is covered by data-dml-approval-recycle-results."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Approval.lock(new Account(Name = 'Acme'));"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Approval.lock(new Account(Name = 'Acme'));"
    ]
  },
  "expected": {
    "error": {
      "type": "Error",
      "message": "DML requires org state"
    }
  }
}
