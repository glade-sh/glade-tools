{
  "name": "async-finalizer-context-getters",
  "evidence": [
    {
      "symbol": "FinalizerContext.getAsyncApexJobId",
      "surfaceId": "apex:System.FinalizerContext.getAsyncApexJobId()",
      "kind": "exec",
      "notes": "Default constructed local finalizer contexts expose empty async job ids."
    },
    {
      "symbol": "FinalizerContextImpl.getAsyncApexJobId",
      "surfaceId": "apex:System.FinalizerContextImpl.getAsyncApexJobId()",
      "kind": "exec",
      "notes": "Default constructed local finalizer context implementations expose empty async job ids."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "System.FinalizerContext fc = new System.FinalizerContext(); System.assertEquals('', fc.getAsyncApexJobId()); System.FinalizerContextImpl fci = new System.FinalizerContextImpl(); System.assertEquals('', fci.getAsyncApexJobId());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "System.FinalizerContext fc = new System.FinalizerContext(); System.assertEquals('', fc.getAsyncApexJobId()); System.FinalizerContextImpl fci = new System.FinalizerContextImpl(); System.assertEquals('', fci.getAsyncApexJobId());"
    ]
  },
  "expected": {
    "ok": true
  }
}
