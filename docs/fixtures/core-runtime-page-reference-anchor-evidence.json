{
  "salesforceEligible": true,
  "name": "core-runtime-page-reference-anchor-evidence",
  "evidence": [
    {
      "surfaceId": "apex:System.PageReference.getAnchor()",
      "symbol": "PageReference.getAnchor",
      "kind": "exec",
      "notes": "getAnchor returns the fragment after # from the PageReference URL."
    },
    {
      "surfaceId": "apex:System.PageReference.setAnchor(String)",
      "symbol": "PageReference.setAnchor",
      "kind": "exec",
      "notes": "setAnchor updates the PageReference anchor and returns the same PageReference."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "PageReference page = new PageReference('/apex/Local?id=1#top');\nSystem.assertEquals('top', page.getAnchor());\nPageReference returned = page.setAnchor('bottom');\nSystem.assertEquals(page, returned);\nSystem.assertEquals('bottom', page.getAnchor());\nSystem.assertEquals('/apex/Local?id=1#bottom', page.getUrl());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "PageReference page = new PageReference('/apex/Local?id=1#top');\nSystem.assertEquals('top', page.getAnchor());\nPageReference returned = page.setAnchor('bottom');\nSystem.assertEquals(page, returned);\nSystem.assertEquals('bottom', page.getAnchor());\nSystem.assertEquals('/apex/Local?id=1#bottom', page.getUrl());"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
