{
  "name": "core-database-upsert-list-object-runtime",
  "salesforceEligible": true,
  "evidence": [
    {
      "surfaceId": "apex:System.Database.upsert(List<Object>)",
      "symbol": "Database.upsert",
      "kind": "exec"
    }
  ],
  "schema": [
    {
      "path": "force-app/main/default/objects/Account/Account.object-meta.xml",
      "content": "<CustomObject><label>Account</label><pluralLabel>Accounts</pluralLabel><sharingModel>ReadWrite</sharingModel></CustomObject>"
    },
    {
      "path": "force-app/main/default/objects/Account/fields/Name.field-meta.xml",
      "content": "<CustomField><fullName>Name</fullName><label>Name</label><type>Text</type><required>true</required></CustomField>"
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "List<SObject> rows = new List<SObject>{new Account(Name = 'Upsert List One'), new Account(Name = 'Upsert List Two')}; List<Database.UpsertResult> results = Database.upsert(rows); System.assertEquals(2, results.size()); System.assert(results[0].isSuccess()); System.assert(results[0].isCreated()); System.assertNotEquals(null, results[0].getId()); System.assert(results[1].isSuccess()); System.assert(results[1].isCreated()); System.assertNotEquals(null, results[1].getId());"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "List<SObject> rows = new List<SObject>{new Account(Name = 'Upsert List One'), new Account(Name = 'Upsert List Two')}; List<Database.UpsertResult> results = Database.upsert(rows); System.assertEquals(2, results.size()); System.assert(results[0].isSuccess()); System.assert(results[0].isCreated()); System.assertNotEquals(null, results[0].getId()); System.assert(results[1].isSuccess()); System.assert(results[1].isCreated()); System.assertNotEquals(null, results[1].getId());"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
