{
  "name": "core-url-accessor-edges",
  "evidence": [
    {
      "symbol": "URL.getAuthority",
      "kind": "exec",
      "notes": "Authority includes user-info when present while host remains hostname-only."
    },
    {
      "symbol": "URL.getHost",
      "kind": "exec",
      "notes": "User-info is excluded from the parsed host accessor."
    },
    {
      "symbol": "URL.getDefaultPort",
      "kind": "exec",
      "notes": "Covers the bounded FTP default port in addition to HTTP/HTTPS."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "URL userInfo = new URL('https://user:pass@example.test/path');\nSystem.assertEquals('user:pass@example.test', userInfo.getAuthority());\nSystem.assertEquals('example.test', userInfo.getHost());\nURL ftp = new URL('ftp://files.example.test/pub/readme.txt');\nSystem.assertEquals(21, ftp.getDefaultPort());\nSystem.assertEquals(-1, new URL('custom://service.example.test/path').getDefaultPort());\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "URL userInfo = new URL('https://user:pass@example.test/path');\nSystem.assertEquals('user:pass@example.test', userInfo.getAuthority());\nSystem.assertEquals('example.test', userInfo.getHost());\nURL ftp = new URL('ftp://files.example.test/pub/readme.txt');\nSystem.assertEquals(21, ftp.getDefaultPort());\nSystem.assertEquals(-1, new URL('custom://service.example.test/path').getDefaultPort());\n"
    ]
  },
  "expected": {
    "stdout": ""
  }
}