{
  "name": "core-numeric-stdlib-invalid-rounding-mode",
  "evidence": [
    {
      "symbol": "RoundingMode.valueOf",
      "kind": "exec",
      "notes": "RoundingMode names are matched exactly; padded names return a stable local error."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "RoundingMode.valueOf(' HALF_UP ');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "RoundingMode.valueOf(' HALF_UP ');"
    ]
  },
  "expected": {
    "error": {
      "type": "Error",
      "message": "unsupported Decimal rounding mode \" HALF_UP \""
    }
  }
}