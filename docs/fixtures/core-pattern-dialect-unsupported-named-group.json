{
  "name": "core-pattern-dialect-unsupported-named-group",
  "evidence": [
    {
      "symbol": "Pattern.compile",
      "kind": "exec",
      "notes": "Python-style named groups accepted by Go regexp are fenced to avoid claiming Java Pattern parity for the wrong dialect."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Pattern.compile('(?P<word>a)');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Pattern.compile('(?P<word>a)');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Pattern.compile Java regex named groups\""
    }
  }
}