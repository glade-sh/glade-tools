{
  "name": "data-platform-search-unsupported-api",
  "evidence": [
    {
      "symbol": "Search.find",
      "kind": "test",
      "notes": "Search.find accepts the supported local SOSL smoke shape."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Search.find('FIND {Acme} IN ALL FIELDS RETURNING Account(Id)');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Search.find('FIND {Acme} IN ALL FIELDS RETURNING Account(Id)');"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
