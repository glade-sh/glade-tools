{
  "name": "core-system-boolean-exact-evidence",
  "salesforceEligible": true,
  "evidence": [
    {
      "surfaceId": "apex:System.Boolean.valueOf(String)",
      "symbol": "Boolean.valueOf",
      "kind": "exec",
      "notes": "Static valueOf converts text to Boolean."
    },
    {
      "surfaceId": "apex:System.Boolean.valueOf(Object)",
      "symbol": "Boolean.valueOf",
      "kind": "exec",
      "notes": "Static valueOf converts supported Object-typed values to Boolean."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Boolean truthy = Boolean.valueOf('true');\nBoolean padded = Boolean.valueOf(' TRUE ');\nObject raw = false;\nBoolean falsy = Boolean.valueOf(raw);\nSystem.assert(truthy);\nSystem.assert(!padded);\nSystem.assert(!falsy);\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Boolean truthy = Boolean.valueOf('true');\nBoolean padded = Boolean.valueOf(' TRUE ');\nObject raw = false;\nBoolean falsy = Boolean.valueOf(raw);\nSystem.assert(truthy);\nSystem.assert(!padded);\nSystem.assert(!falsy);\n"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
