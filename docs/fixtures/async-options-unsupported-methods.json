{
  "name": "async-options-unsupported-methods",
  "evidence": [
    {
      "surfaceId": "apex:System.AsyncOptions.getMaximumQueueableStackDepth()",
      "symbol": "AsyncOptions.getMaximumQueueableStackDepth",
      "kind": "exec",
      "notes": "Local queueable stack option metadata is not modeled."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "AsyncOptions opts = new AsyncOptions(); opts.getMaximumQueueableStackDepth();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "AsyncOptions opts = new AsyncOptions(); opts.getMaximumQueueableStackDepth();"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"AsyncOptions.getMaximumQueueableStackDepth local async options surface\""
    }
  }
}
