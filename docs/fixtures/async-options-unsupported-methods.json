{
  "name": "async-options-maximum-queueable-stack-depth",
  "evidence": [
    {
      "surfaceId": "apex:System.AsyncOptions.getMaximumQueueableStackDepth()",
      "symbol": "AsyncOptions.getMaximumQueueableStackDepth",
      "kind": "exec",
      "notes": "Local queueable stack-depth option metadata is modeled on AsyncOptions."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "AsyncOptions opts = new AsyncOptions(); opts.getMaximumQueueableStackDepth();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "AsyncOptions opts = new AsyncOptions(); opts.getMaximumQueueableStackDepth();"
    ]
  },
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  }
}
