{
  "command": {
    "args": [
      "Database.SaveResult result = EventBus.publish(new Event_Recipes_Demo__e()); System.assert(result.isSuccess()); List<Database.SaveResult> many = EventBus.publish(new List<SObject>{new Event_Recipes_Demo__e(), new Event_Recipes_Demo__e()}); System.assertEquals(2, many.size()); System.assert(many[0].isSuccess()); System.assert(many[1].isSuccess());"
    ],
    "kind": "exec"
  },
  "evidence": [
    {
      "kind": "exec",
      "notes": "Local platform event publish returns no-op success SaveResult records; delivery is not modeled.",
      "symbol": "EventBus.publish"
    },
    {
      "kind": "exec",
      "notes": "Local EventBus.publish returns no-delivery SaveResult values.",
      "surfaceId": "apex:System.EventBus.publish(Object)",
      "symbol": "EventBus.publish(Object)"
    },
    {
      "kind": "exec",
      "notes": "Local EventBus.publish returns no-delivery SaveResult values.",
      "surfaceId": "apex:System.EventBus.publish(List<Object>)",
      "symbol": "EventBus.publish(List<Object>)"
    }
  ],
  "expected": {
    "result": {
      "debug": null,
      "ok": true
    }
  },
  "name": "integration-eventbus-local-publish",
  "salesforceEligible": false,
  "salesforceExclusionClass": "org-configuration-required",
  "salesforceExclusionReason": "API 67 clean-org cannot resolve the fixture's local custom event type; retained as explicit negative/local evidence and excluded from hosted parity.",
  "schema": [
    {
      "content": "<CustomObject><label>Event Recipes Demo</label><pluralLabel>Event Recipes Demos</pluralLabel><deploymentStatus>Deployed</deploymentStatus><eventType>HighVolume</eventType></CustomObject>",
      "path": "force-app/main/default/objects/Event_Recipes_Demo__e/Event_Recipes_Demo__e.object-meta.xml"
    }
  ],
  "source": [
    {
      "content": "Database.SaveResult result = EventBus.publish(new Event_Recipes_Demo__e()); System.assert(result.isSuccess()); List<Database.SaveResult> many = EventBus.publish(new List<SObject>{new Event_Recipes_Demo__e(), new Event_Recipes_Demo__e()}); System.assertEquals(2, many.size()); System.assert(many[0].isSuccess()); System.assert(many[1].isSuccess());",
      "path": "anonymous.apex"
    }
  ]
}
